spring:
  application:
    name: travel-management-eureka-server
  cloud:
    config:
      fail-fast: true
      username: root
      password: root
---
spring:
  profiles: local
  cloud:
    config:
      enabled: false